diff --git a/configure b/configure
index 6ebeab30fe3..949fda07103 100755
--- a/configure
+++ b/configure
@@ -2269,17 +2269,17 @@ while [ "$#" -gt 0 ]; do
         ;;
     c++std)
         case "$VAL" in
-            c++98|c++11|c++14|c++1z|auto)
+            c++98|c++11|c++14|c++1z|c++20|auto)
                 CFG_STDCXX="$VAL"
                 ;;
-            98|11|14|1z)
+            98|11|14|1z|20)
                 CFG_STDCXX="c++$VAL"
                 ;;
             1y|c++1y)
                 CFG_STDCXX="c++14"
                 ;;
             *)
-                echo >&2 "Invalid C++ edition: $VAL; valid options are: c++98 c++11 c++14 c++1z auto"
+                echo >&2 "Invalid C++ edition: $VAL; valid options are: c++98 c++11 c++14 c++1z c++20 auto"
                 ERROR=yes
                 ;;
         esac
@@ -2458,7 +2458,7 @@ Configure options:
     -confirm-license ... Automatically acknowledge the license (use with
                          either -opensource or -commercial)
 
-    -c++std <edition> .. Compile Qt with C++ standard edition (c++98, c++11, c++14, c++1z)
+    -c++std <edition> .. Compile Qt with C++ standard edition (c++98, c++11, c++14, c++1z, c++20)
                          Default: highest supported
 
  *  -shared ............ Create and use shared Qt libraries.
@@ -4426,8 +4426,16 @@ elif ! compileTest common/c++1z "C++1z"; then
     else
         CFG_STDCXX="c++14"
     fi
+elif [ "$CFG_STDCXX" = "c++1z" ]; then
+    : # CFG_STDCXX is correct
+elif ! compileTest common/c++20 "C++20"; then
+    if [ "$CFG_STDCXX" != "auto" ]; then
+        stdcxx_error=true
+    else
+        CFG_STDCXX="c++1z"
+    fi
 else
-    CFG_STDCXX="c++1z"
+    CFG_STDCXX="c++20"
 fi
 
 if $stdcxx_error && [ "$CFG_CONFIGURE_EXIT_ON_ERROR" = "yes" ]; then
@@ -6213,6 +6221,9 @@ if [ "$CFG_STDCXX" != "c++98" ]; then
         QT_CONFIG="$QT_CONFIG c++14"
         if [ "$CFG_STDCXX" != "c++14" ]; then
             QT_CONFIG="$QT_CONFIG c++1z"
+            if [ "$CFG_STDCXX" != "c++1z" ]; then
+                QT_CONFIG="$QT_CONFIG c++20"
+            fi
         fi
     fi
 fi
diff --git a/mkspecs/common/clang.conf b/mkspecs/common/clang.conf
index e003b947aab..95180537575 100644
--- a/mkspecs/common/clang.conf
+++ b/mkspecs/common/clang.conf
@@ -29,9 +29,11 @@ QMAKE_CXXFLAGS_DISABLE_LTCG      = $$QMAKE_CFLAGS_DISABLE_LTCG
 QMAKE_CXXFLAGS_CXX11             = -std=c++11
 QMAKE_CXXFLAGS_CXX14             = -std=c++1y
 QMAKE_CXXFLAGS_CXX1Z             = -std=c++1z
+QMAKE_CXXFLAGS_CXX20             = -std=c++20
 QMAKE_CXXFLAGS_GNUCXX11          = -std=gnu++11
 QMAKE_CXXFLAGS_GNUCXX14          = -std=gnu++1y
 QMAKE_CXXFLAGS_GNUCXX1Z          = -std=gnu++1z
+QMAKE_CXXFLAGS_GNUCXX20          = -std=gnu++20
 
 QMAKE_LFLAGS_CXX11      =
 QMAKE_LFLAGS_CXX14      =
diff --git a/mkspecs/common/g++-base.conf b/mkspecs/common/g++-base.conf
index 42545519947..4c911d0bc50 100644
--- a/mkspecs/common/g++-base.conf
+++ b/mkspecs/common/g++-base.conf
@@ -30,9 +30,12 @@ QMAKE_CXXFLAGS_USE_PRECOMPILE = $$QMAKE_CFLAGS_USE_PRECOMPILE
 QMAKE_CXXFLAGS_CXX11    = -std=c++0x
 QMAKE_CXXFLAGS_CXX14    = -std=c++1y
 QMAKE_CXXFLAGS_CXX1Z    = -std=c++1z
+QMAKE_CXXFLAGS_CXX20    = -std=c++20
 QMAKE_CXXFLAGS_GNUCXX11 = -std=gnu++0x
 QMAKE_CXXFLAGS_GNUCXX14 = -std=gnu++1y
 QMAKE_CXXFLAGS_GNUCXX1Z = -std=gnu++1z
+QMAKE_CXXFLAGS_GNUCXX20 = -std=gnu++20
 QMAKE_LFLAGS_CXX11      =
 QMAKE_LFLAGS_CXX14      =
 QMAKE_LFLAGS_CXX1Z      =
+QMAKE_LFLAGS_CXX20      =
diff --git a/mkspecs/common/gcc-base-mac.conf b/mkspecs/common/gcc-base-mac.conf
index e9bf780ec1c..ff851d436af 100644
--- a/mkspecs/common/gcc-base-mac.conf
+++ b/mkspecs/common/gcc-base-mac.conf
@@ -12,6 +12,10 @@ include(gcc-base.conf)
 
 QMAKE_COMPILER_DEFINES += __APPLE__ __GNUC__=4 __APPLE_CC__
 
+# On ARM64 macOS, NEON is always available and doesn't need compiler flags
+QMAKE_CFLAGS_NEON       =
+QMAKE_CXXFLAGS_NEON     =
+
 QMAKE_LFLAGS           += -headerpad_max_install_names
 
 QMAKE_LFLAGS_SHLIB         += -single_module -dynamiclib
diff --git a/mkspecs/features/default_post.prf b/mkspecs/features/default_post.prf
index 561c8f4858c..2c7c9a35e72 100644
--- a/mkspecs/features/default_post.prf
+++ b/mkspecs/features/default_post.prf
@@ -98,8 +98,9 @@ breakpad {
 # Disable special compiler flags for host builds (needs to be changed for 5.7
 # to fall back to c++11 because since 5.7 c++11 is required everywhere,
 # including host builds).
-if(!host_build|!cross_compile):if(c++11|c++14|c++1z) {
-    c++1z:       cxxstd = CXX1Z
+if(!host_build|!cross_compile):if(c++11|c++14|c++1z|c++20) {
+    c++20:       cxxstd = CXX20
+    else: c++1z: cxxstd = CXX1Z
     else: c++14: cxxstd = CXX14
     else:        cxxstd = CXX11
 
diff --git a/src/3rdparty/libpng/pngpriv.h b/src/3rdparty/libpng/pngpriv.h
index c06deee68d7..7b044733a46 100644
--- a/src/3rdparty/libpng/pngpriv.h
+++ b/src/3rdparty/libpng/pngpriv.h
@@ -430,7 +430,7 @@
 #  include <float.h>
 
 #  if (defined(__MWERKS__) && defined(macintosh)) || defined(applec) || \
-    defined(THINK_C) || defined(__SC__) || defined(TARGET_OS_MAC)
+    defined(THINK_C) || defined(__SC__)
      /* We need to check that <math.h> hasn't already been included earlier
       * as it seems it doesn't agree with <fp.h>, yet we should really use
       * <fp.h> if possible.
diff --git a/src/corelib/global/qtypetraits.h b/src/corelib/global/qtypetraits.h
index 488e257e0f1..157d16fc1c8 100644
--- a/src/corelib/global/qtypetraits.h
+++ b/src/corelib/global/qtypetraits.h
@@ -99,11 +99,15 @@
 //  - Enclose in QtPrivate namespace.
 
 #include "QtCore/qglobal.h"
+#include "qglobal.h"
 
 #ifndef QTYPETRAITS_H
 #define QTYPETRAITS_H
 
 #include <utility>                  // For pair
+#if __cplusplus >= 201103L || defined(__GXX_EXPERIMENTAL_CXX0X__)
+#  include <type_traits>
+#endif
 
 QT_BEGIN_NAMESPACE
 
@@ -473,9 +477,14 @@ struct not_c
     : integral_constant<bool, !B> {};
 
 // Checks whether a type is unsigned (T must be convertible to unsigned int):
+#if __cplusplus >= 201103L || defined(__GXX_EXPERIMENTAL_CXX0X__)
+template <typename T>
+struct is_unsigned : integral_constant<bool, std::is_unsigned<T>::value> {};
+#else
 template <typename T>
 struct is_unsigned
     : integral_constant<bool, (T(0) < T(-1))> {};
+#endif
 
 // Checks whether a type is signed (T must be convertible to int):
 template <typename T>
diff --git a/src/corelib/plugin/qmachparser.cpp b/src/corelib/plugin/qmachparser.cpp
index d1ac91c7d54..10c7543b02d 100644
--- a/src/corelib/plugin/qmachparser.cpp
+++ b/src/corelib/plugin/qmachparser.cpp
@@ -53,6 +53,9 @@ static const cpu_type_t my_cputype = CPU_TYPE_X86;
 static const cpu_type_t my_cputype = CPU_TYPE_POWERPC64;
 #elif defined(Q_PROCESSOR_POWER_32)
 static const cpu_type_t my_cputype = CPU_TYPE_POWERPC;
+#elif defined(Q_PROCESSOR_ARM_64)
+#  define MACHO64
+static const cpu_type_t my_cputype = CPU_TYPE_ARM64;
 #elif defined(Q_PROCESSOR_ARM)
 static const cpu_type_t my_cputype = CPU_TYPE_ARM;
 #else
diff --git a/src/gui/painting/painting.pri b/src/gui/painting/painting.pri
index 1e516e45814..a64029812f4 100644
--- a/src/gui/painting/painting.pri
+++ b/src/gui/painting/painting.pri
@@ -103,8 +103,9 @@ AVX2_SOURCES += painting/qdrawhelper_avx2.cpp
 
 NEON_SOURCES += painting/qdrawhelper_neon.cpp
 NEON_HEADERS += painting/qdrawhelper_neon_p.h
-NEON_ASM += ../3rdparty/pixman/pixman-arm-neon-asm.S painting/qdrawhelper_neon_asm.S
-!ios:!contains(QT_ARCH, "arm64"): DEFINES += ENABLE_PIXMAN_DRAWHELPERS
+# ARM32 assembly doesn't work on ARM64 or iOS
+!ios:!macos: NEON_ASM += ../3rdparty/pixman/pixman-arm-neon-asm.S painting/qdrawhelper_neon_asm.S
+!ios:!macos: DEFINES += ENABLE_PIXMAN_DRAWHELPERS
 
 MIPS_DSP_SOURCES += painting/qdrawhelper_mips_dsp.cpp
 MIPS_DSP_HEADERS += painting/qdrawhelper_mips_dsp_p.h painting/qt_mips_asm_dsp_p.h
diff --git a/src/platformsupport/fontdatabases/mac/qfontengine_coretext.mm b/src/platformsupport/fontdatabases/mac/qfontengine_coretext.mm
index 7b459584ea3..e5861c532d9 100644
--- a/src/platformsupport/fontdatabases/mac/qfontengine_coretext.mm
+++ b/src/platformsupport/fontdatabases/mac/qfontengine_coretext.mm
@@ -764,7 +764,7 @@ void QCoreTextFontEngine::getUnscaledGlyph(glyph_t glyph, QPainterPath *path, gl
 
 QFixed QCoreTextFontEngine::emSquareSize() const
 {
-    return QFixed::QFixed(int(CTFontGetUnitsPerEm(ctfont)));
+    return QFixed(int(CTFontGetUnitsPerEm(ctfont)));
 }
 
 QFontEngine *QCoreTextFontEngine::cloneWithSize(qreal pixelSize) const
diff --git a/src/plugins/platforms/cocoa/qcocoahelpers.mm b/src/plugins/platforms/cocoa/qcocoahelpers.mm
index cd731487529..fe42c7d4d26 100644
--- a/src/plugins/platforms/cocoa/qcocoahelpers.mm
+++ b/src/plugins/platforms/cocoa/qcocoahelpers.mm
@@ -549,9 +549,9 @@ OSStatus qt_mac_drawCGImage(CGContextRef inContext, const CGRect *inBounds, CGIm
     // Verbatim copy if HIViewDrawCGImage (as shown on Carbon-Dev)
     OSStatus err = noErr;
 
-    require_action(inContext != NULL, InvalidContext, err = paramErr);
-    require_action(inBounds != NULL, InvalidBounds, err = paramErr);
-    require_action(inImage != NULL, InvalidImage, err = paramErr);
+    if (inContext == NULL) { err = paramErr; goto InvalidContext; }
+    if (inBounds == NULL) { err = paramErr; goto InvalidBounds; }
+    if (inImage == NULL) { err = paramErr; goto InvalidImage; }
 
     CGContextSaveGState( inContext );
     CGContextTranslateCTM (inContext, 0, inBounds->origin.y + CGRectGetMaxY(*inBounds));
diff --git a/src/testlib/qtesttable.cpp b/src/testlib/qtesttable.cpp
index 05cce37a47d..be3f484a74c 100644
--- a/src/testlib/qtesttable.cpp
+++ b/src/testlib/qtesttable.cpp
@@ -132,9 +132,12 @@ QTestData *QTestTable::testData(int index) const
     return size_t(index) < d->dataList.size() ? d->dataList[index] : Q_NULLPTR;
 }
 
-class NamePredicate : public std::unary_function<QTestTablePrivate::Element, bool>
+class NamePredicate
 {
 public:
+    typedef QTestTablePrivate::Element argument_type;
+    typedef bool result_type;
+
     explicit NamePredicate(const char *needle) : m_needle(needle) {}
 
     bool operator()(const QTestTablePrivate::Element &e) const
diff --git a/src/tools/qlalr/compress.cpp b/src/tools/qlalr/compress.cpp
index fa67504a088..7360d6b2a44 100644
--- a/src/tools/qlalr/compress.cpp
+++ b/src/tools/qlalr/compress.cpp
@@ -42,16 +42,24 @@
 
 #define QLALR_NO_CHECK_SORTED_TABLE
 
-struct _Fit: public std::binary_function<int, int, bool>
+struct _Fit
 {
+  typedef int first_argument_type;
+  typedef int second_argument_type;
+  typedef bool result_type;
+
   inline bool operator () (int a, int b) const
   {
     return a == 0 || b == 0 || a == b;
   }
 };
 
-struct _PerfectMatch: public std::binary_function<int, int, bool>
+struct _PerfectMatch
 {
+  typedef int first_argument_type;
+  typedef int second_argument_type;
+  typedef bool result_type;
+
   inline bool operator () (int a, int b) const
   { return a == b; }
 };
@@ -140,8 +148,12 @@ private:
   const_iterator _M_endNonZeros;
 };
 
-struct _SortUncompressedRow: public std::binary_function<UncompressedRow, UncompressedRow, bool>
+struct _SortUncompressedRow
 {
+  typedef UncompressedRow first_argument_type;
+  typedef UncompressedRow second_argument_type;
+  typedef bool result_type;
+
   inline bool operator () (const UncompressedRow &a, const UncompressedRow &b) const
   { return a.count (0) > b.count (0); }
 };
diff --git a/src/tools/qlalr/lalr.cpp b/src/tools/qlalr/lalr.cpp
index 537051956e9..65543a546b3 100644
--- a/src/tools/qlalr/lalr.cpp
+++ b/src/tools/qlalr/lalr.cpp
@@ -238,8 +238,11 @@ void Grammar::buildExtendedGrammar ()
   non_terminals.insert (accept_symbol);
 }
 
-struct Nullable: public std::unary_function<Name, bool>
+struct Nullable
 {
+  typedef Name argument_type;
+  typedef bool result_type;
+
   Automaton *_M_automaton;
 
   Nullable (Automaton *aut):
